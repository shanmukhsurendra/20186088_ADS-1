insertion sort would be faster than the selection sort when all the keys are identical in an array because when an array has identical keys then it means it is a sorted array for which the insertion sort runs linear. 
But the selection sort is always quadratic what ever the scenario. 